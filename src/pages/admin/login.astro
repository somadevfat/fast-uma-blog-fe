---
export const prerender = false;
import AdminLayout from "../../layouts/AdminLayout.astro";
import { API_BASE_URL } from "../../lib/api";

// Redirect if already logged in (Server side check could be here, but let's simple first)
---

<AdminLayout title="ログイン">
    <div class="max-w-md mx-auto mt-20">
        <div class="bg-white border border-wiki-border shadow-sm">
            <div
                class="bg-wiki-header-bg border-b border-wiki-border px-6 py-3 font-bold"
            >
                管理者認証
            </div>

            <div class="p-8 space-y-6">
                <div class="text-center mb-6">
                    <p class="text-xs text-gray-500">
                        Wikiの編集を行うには管理権限が必要です。
                    </p>
                </div>

                <form id="login-form" class="space-y-6">
                    <div class="space-y-1">
                        <label class="text-xs font-bold text-gray-600"
                            >ユーザー名</label
                        >
                        <input
                            type="text"
                            name="username"
                            required
                            class="w-full bg-gray-50 border border-wiki-border px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-primary"
                            placeholder="admin"
                        />
                    </div>

                    <div class="space-y-1">
                        <label class="text-xs font-bold text-gray-600"
                            >パスワード</label
                        >
                        <input
                            type="password"
                            name="password"
                            required
                            class="w-full bg-gray-50 border border-wiki-border px-3 py-2 text-sm focus:outline-none focus:ring-1 focus:ring-primary"
                            placeholder="••••••••"
                        />
                    </div>

                    <div
                        id="error-message"
                        class="text-red-600 text-xs font-bold hidden text-center border border-red-200 bg-red-50 p-2"
                    >
                        認証失敗: ユーザー名またはパスワードが正しくありません。
                    </div>

                    <button
                        type="submit"
                        class="w-full bg-primary text-white font-bold text-sm py-3 hover:opacity-90 transition-opacity"
                    >
                        認証を開始する
                    </button>
                </form>
            </div>
        </div>

        <div class="mt-8 text-center text-xs">
            <a
                href="/"
                class="text-gray-500 hover:text-primary transition-colors hover:underline"
                >&laquo; 公開Wikiへ戻る</a
            >
        </div>
    </div>
</AdminLayout>

<script define:vars={{ API_BASE_URL }}>
    const form = document.getElementById("login-form");
    const errorMsg = document.getElementById("error-message");

    form?.addEventListener("submit", async (e) => {
        e.preventDefault();
        errorMsg?.classList.add("hidden");

        const formData = new FormData(e.target);
        const data = Object.fromEntries(formData);

        try {
            const res = await fetch(`${API_BASE_URL}/auth/login`, {
                method: "POST",
                headers: { "Content-Type": "application/json" },
                body: JSON.stringify(data),
            });

            const result = await res.json();
            if (result.success) {
                window.location.href = "/admin";
            } else {
                errorMsg?.classList.remove("hidden");
            }
        } catch (err) {
            console.error("Auth error", err);
            alert("Connection failure");
        }
    });
</script>
